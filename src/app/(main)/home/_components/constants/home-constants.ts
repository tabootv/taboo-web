/**
 * Home component constants
 */

export const HOVER_DELAY = 300; // Delay before showing video preview on hover (ms)
export const VIDEO_PREVIEW_DELAY = 500; // Delay before starting video preview (ms)

